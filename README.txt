1 - Fazer o dump do sql que existe neste arquivo zipado
2 - dentro do diretorio application/config/database.php
	Alterar as linhas 78 79, 80 e 81, com as defidas informações de acesso ao local de acesso ao db que foi gerado na etapa anterior
3 - acessar o diretorio root (PI V) através do terminal, então executar o seguinte comando: php -S localhost:8080
4 - acessar o browser informando a url localhost:8080
